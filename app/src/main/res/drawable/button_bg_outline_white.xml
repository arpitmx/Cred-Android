<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background color -->
    <stroke android:color="@color/primary_text"
        android:width="1dp"
        />
    <corners
       android:radius="50dp" />
</shape>
